# HR_job_posting
AI-powered HR portal and candidate application system built with FastAPI. Create shareable apply links, accept PDF resumes (kept with original filenames), auto-run ATS scoring via Google Generative AI, review applicants in a clean HR UI, view full details in a modal, accept/reject with SMTP email notifications, and export everything to Excel.
